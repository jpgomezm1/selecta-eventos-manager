project_id = "xvvbxyjcieckbbdcuoge"

# Todas las funciones exigen JWT (el default). No hay ningún endpoint público
# sin autenticar: los soportes de pago se sincronizan por cron desde
# `sincronizar-soportes-pago`, que además comprueba que quien llama traiga la
# service role key — `verify_jwt` por sí solo deja pasar la anon key, que viaja
# en el bundle del frontend.
